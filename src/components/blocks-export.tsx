import { EmptyBlock, ErrorBlock, LoadingBlock } from "@/components/Blocks";

/** Convenience namespace: Blocks.Loading / Blocks.Empty / Blocks.Error */
export const Blocks = {
  Loading: LoadingBlock,
  Empty: EmptyBlock,
  Error: ErrorBlock,
};